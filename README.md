# Wheel-of-fortune
“Wheel of Fortune” is a simple interactive web application, which was developed with the aim of being used at trade fairs on a single device.
All collected data is stored locally.

In the admin panel, it is possible to: view how many and which prizes have been won, download a CSV file containing the stored data, and reset the prize count.

